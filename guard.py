import pygame
from maze import Maze
from char import Char

class Guard:

    def __init__(self)
        self.position_x = position_x
        self.position_y = position_y
        self.x = position_x * spirte_size
        self.y = position_y * sprite_size
        self.max_itmes_nb = 3
	
    def items_check(self)
        if (char.items_counter) == self.max_items_nb:
           return True
	
