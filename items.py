import random
from laby import *

class Items:

    def __init__(self, position_x, position_y):
        self.item = item
        self.position_x = []
    
    def generer_objets(self)
        # aiguille = "image de l'aiguille"
        # tube en plastique = "image du tube en plastique
        # ether = "image de l'ether"
        
#        for i, line in enumerate(structure):
#            for j, column in enumerate(line):
#                if column == 0:
#                   position_x = random.randrange(1, 15)
#                   position_y = random.randrange(1, 15)

        
